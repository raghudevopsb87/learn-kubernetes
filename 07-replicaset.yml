apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: rs01
  labels:
    app: guestbook
    tier: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
      - name: nginx
        image: nginx

---
apiVersion: v1
kind: Pod
metadata:
  name: same-label-as-rs
spec:
  containers:
  - name: nginx
    image: nginx


