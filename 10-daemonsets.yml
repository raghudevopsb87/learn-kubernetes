apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemon01
  labels:
    app: guestbook
    tier: daemon01
spec:
  selector:
    matchLabels:
      tier: daemon01
  template:
    metadata:
      labels:
        tier: daemon01
    spec:
      containers:
      - name: nginx
        image: nginx:1.27

